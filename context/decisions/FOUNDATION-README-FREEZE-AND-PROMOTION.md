# FOUNDATION-README-FREEZE-AND-PROMOTION

## CONTEXT

`phases/backlog/foundation/README.md` was reviewed, rewritten to match the live foundation contract, and then explicitly approved under the global freeze rule for freeze and promotion.

## OPTIONS

- Keep `phases/backlog/foundation/README.md` in `DRAFT` state and continue review later.
- Freeze `phases/backlog/foundation/README.md` now as an in-place canonical file.

## DECISION

`phases/backlog/foundation/README.md` is frozen.

Because it is an in-place canonical file, promotion does not require a move.
Promotion means:
- its status changes from `DRAFT` to `FROZEN`
- it becomes active repo truth

## APPROVAL

EXPLICIT
